import React from "react";

const Header = () => {
  return (
    <nav className="navbar navbar-dark bg-dark">
      <div className="container fluid">
        <h1 className="navbae-brand text-white">Agenda React + Reducer</h1>
      </div>
    </nav>
  );
};

export default Header;
